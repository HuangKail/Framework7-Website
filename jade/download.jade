extends _base
block var
  - var headerHighlight = 'download'
block body
  h1 下载 SUI-Mobile

  .important-note
    ul
      li 注意，建议直接使用我们的CDN服务器，而不要自己下载到本地。
      li 注意不要使用本服务器上的资源，这只是在官网上使用的。

  h2 使用阿里CDN

  p 建议直接引用我们的CDN地址：

  h4 压缩版
  p 适用于线上环境
  ul.list
    li 
      a(href='#{jsMinLink}' target='_blank') #{jsMinLink}
    li
      a(href='#{cssMinLink}' target='_blank') #{cssMinLink}

  h4 非压缩版
  p 适用于开发环境，方便调试。
  ul.list
    li 
      a(href='#{jsLink}' target='_blank') #{jsLink}
    li
      a(href='#{cssLink}' target='_blank') #{cssLink}

  p 你会注意到在上述地址中有一个类似 <strong>x.y.z</strong> 的路径，这是我们的版本号。为了保证稳定性，我们每次发布都是增量发布：即每次都会递增版本号，已经发布的版本号不会再有任何改动。

  h2 下载源码

  p 我们的源码托管在 github 上，地址：<a href='https://github.com/sdc-alibaba/SUI-Mobile'>https://github.com/sdc-alibaba/SUI-Mobile</a>
  p 执行 <code>git clone git@github.com:sdc-alibaba/SUI-Mobile.git</code> 来克隆完整的源码。
